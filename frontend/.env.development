# Whether to open mock（改造：关闭 mock，走真实 Flask 后端）
VITE_USE_MOCK = false

# public path
VITE_PUBLIC_PATH = /

# Basic interface address SPA（改造：对齐 Flask 的 /api 前缀）
VITE_GLOB_API_URL = /api

# File upload address， optional
VITE_GLOB_UPLOAD_URL = /upload

# Interface prefix
VITE_GLOB_API_URL_PREFIX =
